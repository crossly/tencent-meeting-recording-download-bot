# ============================================
# Tencent Meeting Recording Downloader
# Environment Configuration
# ============================================
#
# Docker Usage:
#   1. Copy this file to .env: cp .env.example .env
#   2. Fill in your credentials below
#   3. Run: docker-compose up -d
#
# ============================================

# --- RUN MODE ---
# "BOT" or "CLIENT"
# Note: CLIENT mode requires interactive login on first run
RUN_MODE=CLIENT

# --- TELEGRAM BOT CONFIG (Used if RUN_MODE == BOT) ---
TELEGRAM_TOKEN=

# --- TELEGRAM CLIENT CONFIG (Used if RUN_MODE == CLIENT) ---
# Get these from https://my.telegram.org
TG_API_ID=
TG_API_HASH=
# StringSession for containerized environments (no interactive login needed)
# Generate this by running: python generate_session.py
# Then paste the output string here
TG_SESSION_STRING=

# --- DOWNLOADER CONFIG ---
# Your Tencent Meeting session cookie
DEFAULT_COOKIE=
